# Simple command
# This will open the editor that you have by default on your terminal
git commit

# This avoids using the editor
git commit -m "This is the message from the commit"

# Used when something has changed and you don't want to create another commit
# This can happend when the changes are typos or you want to change the commit message}
# It will open the editor that you have by deault on your terminal
git commit --amend